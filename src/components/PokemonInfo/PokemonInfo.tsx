import React from 'react';
import { PokemonDataProps } from './interface';

const PokemonInfo = ({ pokemonData }: PokemonDataProps) => {
  const name = pokemonData?.name;

  return (
    <>
      <div></div>
    </>
  );
};

export default PokemonInfo;
