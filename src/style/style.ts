import { colors, alignments } from './index';

export const leftSideStyle = {
  display: 'center',
  justifyContent: 'flex-start',
  alignItems: 'center',
  FlexDirection: 'column',
  width: '800px',
  height: '100vh',
};
